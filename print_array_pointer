#include <stdio.h>
int main()
{
   int a[]={1,2,3,4};
   int n;
   int *p;
   int i;
   n=sizeof(a)/sizeof(a[0]);
   printf("%d",n);
   
   p=a;
   for(i=0;i<n;i++)
   {
       printf("%d",*(p+i));
   }
}
